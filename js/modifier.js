document.addEventListener("DOMContentLoaded", function(){
    const backLogin = document.querySelector("#login");
    const logo = document.querySelector("body.login h1 a");

    /*const styles = getComputedStyle(logo);
    const color = styles.color;
    backLogin.style.backgroundColor = color;

})